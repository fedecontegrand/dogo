const { User } = require('../db');
const { Pet } = require('../db');

async function getUser(req, res, next) {
    try {

    } catch (error) {
        next(error)
    }
};

module.exports = { getUser };